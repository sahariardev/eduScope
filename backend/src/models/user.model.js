class User {
    constructor(id, email, name, password, isAdmin) {
        this.id = id;
        this.email = email;
        this.name = name;
        this.password = password;
        this.isAdmin = isAdmin;
    }
}

export default User;